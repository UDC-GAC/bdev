This tool can be used to gather Energy measurements on Intel CPUs from Sandy Bridge onwards using the RAPL interface.

Be sure to configure PAPI with --with-components="rapl" and have read permissions on the /dev/cpu/*/msr files.
